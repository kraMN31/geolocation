apiVersion: apps/v1
kind: Deployment
metadata:
  name: eks-cluster-ecr-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: eks-cluster-ecr-app
  template:
    metadata:
      labels:
        app: eks-cluster-ecr-app
    spec:
      containers:
      - name: eks-cluster-ecr
        image: 740955001227.dkr.ecr.us-east-1.amazonaws.com/eks-cluster-ecr:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8082
# service type loadbalancer       
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: eks-cluster-ecr-app
    k8s-app: eks-cluster-ecr-app
  name: eks-cluster-ecr-app
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8082
  type: LoadBalancer
  selector:
    app: eks-cluster-ecr-app